import 'package:flutter/material.dart';

class Concern extends StatelessWidget {
  const Concern({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Concern');
  }
}
